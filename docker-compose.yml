version: '3.8'

services:
  postgres:
    container_name: vaga_manager_postgres
    image: postgres
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=vaga_manager_db